Your work is not finished. Read and execute the below with ultrathink.

## Original Implementation Plan

**IMPORTANT**: Before proceeding, review the original plan you are implementing:
@docs/plan/01-vector-add.md

This plan contains the full scope of work and requirements. Ensure your work aligns with this plan.

---

For all tasks that need to be completed, please use the Task system (TaskCreate, TaskUpdate, TaskList) to track each item in order of importance.
You are strictly prohibited from only addressing the most important issues - you MUST create Tasks for ALL discovered issues and attempt to resolve each one.

Before executing each task in this round:
1. Read @/mnt/home-nas/work/repo/FlagLang/.humanize/bitlesson.md
2. Run `bitlesson-selector` for each task/sub-task
3. Follow selected lesson IDs (or `NONE`) during implementation

---
Below is Codex's review result:
<!-- CODEX's REVIEW RESULT START -->
# Round 1 Review Result

## Findings

### [P0] AC-6 is still not met: PTX emission trusts an AST-created descriptor instead of the lowered native/affine/NTT IR

Claude removed the old `name == "add_kernel"` escape hatch, but replaced it with a descriptor created from the Python AST in `ast_to_ttir` before any backend stage validates the compiled module shape:

- `python/triton/compiler/code_generator.py:1629-1637`
- `python/triton/compiler/code_generator.py:1647-1745`
- `third_party/nvidia/backend/compiler.py:137-207`
- `third_party/nvidia/backend/compiler.py:590-600`

This does not satisfy the round-0 directive or the original AC-6 goal. The recognizer is not inspecting the post-`add_optimize_ttir` module for two affine gathers, one fadd, one affine scatter, pointer bases, mask/default semantics, or the actual affine relation. It only proves the Python source text matched one narrow spelling. A broken affine rewrite, broken NTT lowering, or mutated native module can still reach `make_ptx` as long as `_flaglang_vector_add` is present.

`make_ttgir` and `make_llir` still return the module unchanged (`third_party/nvidia/backend/compiler.py:412-513`, `third_party/nvidia/backend/compiler.py:515-521`), so the successful tutorial path remains disconnected from a validated nncase/NTT-derived codegen contract.

Directive implementation plan:

1. Stop using the AST recognizer as the authority for CUDA emission. It may remain only as an optional prefilter or diagnostic, but `make_ptx` must not accept a module solely because `_flaglang_vector_add` was attached during AST parsing.
2. After `make_ttir` runs `pm.add_optimize_ttir(capability)`, inspect the actual native module and derive the vector-add contract from that module. If Python lacks read APIs, add a minimal native binding in `python/flaglang/native/ir.cpp` that returns the entry function, argument types/order, operation list, affine gather/scatter operands, relation domains/symbols/results/constraint, load defaults, compute op, and scatter destination.
3. Validate exactly one supported shape: three float32 pointer args plus `n_elements`, two masked affine gathers from distinct input pointer bases, one floating add, one masked affine scatter to the output pointer, shared relation `program_id(0) * BLOCK_SIZE + d0`, lane range `[0, BLOCK_SIZE - 1]`, and constraint `< n_elements`.
4. Store the emission descriptor only after that post-TTIR/native validation succeeds. `make_ptx` should consume only this validated descriptor and reject missing, fake, or stale descriptors.
5. Add a negative test proving a fake object with a syntactically valid `_flaglang_vector_add` descriptor cannot emit PTX unless the actual module inspection succeeds.
6. Keep PTX emission narrow to vector add, but tie it to the compiled IR rather than to Python AST spelling.

### [P1] AC-6 dump artifacts are descriptor summaries, not actual stage dumps

The new dump text is deterministic, but it is generated by copying fields from `_flaglang_vector_add`:

- `python/triton/compiler/compiler.py:143-166`

Those `ntt.affine.gather` and `ntt.affine.scatter` markers are not serialized from the `.ttir`, `.ttgir`, or `.llir` stage output. The tests therefore prove only that descriptor text was written, not that the actual lowered IR contains the expected affine/NTT structure.

Directive implementation plan:

1. Implement or expose a real native module serializer for the stage object, and call it from `_serialize_ir_for_storage`.
2. Dump actual stage output for `.ttir`, `.ttgir`, and `.llir`. If a stage is currently identity, its dump may be identical, but it must still come from the module serializer, not descriptor JSON.
3. Remove descriptor-generated fake op markers from dump validation. Tests should assert real serialized IR contains the entry name and affine/NTT operations produced by the compiler pipeline.

### [P1] Claimed CUDA validation was not reproducible in this review environment

The current review run could not reproduce Claude's claimed CUDA-dependent validation:

- `conda run -n flaglang python -m pytest python/test/unit/runtime/test_flaglang_cuda_backend.py python/test/unit/runtime/test_flaglang_native_ir.py -q` produced `6 passed, 2 skipped`, not `8 passed`; the skipped tests are CUDA-dependent.
- `conda run -n flaglang python -c "import torch; print(torch.__version__, torch.cuda.is_available())"` printed `2.11.0+cu128 False`.
- `nvidia-smi` failed because it could not communicate with the NVIDIA driver.

This may be an environment availability issue rather than a code regression, but AC-2 and the forced tutorial portions of AC-6 cannot be accepted from this review run. Task13 must be rerun when CUDA is available.

## Goal Alignment Summary

ACs: 4/6 addressed; AC-2 is currently environment-blocked and AC-6 is incomplete | Forgotten items: 2 | Unjustified deferrals: 0

- AC-1: Import path and `ldd` checks passed in review.
- AC-2: Previously claimed, but currently not reproducible because CUDA is unavailable.
- AC-3: No new concerns found.
- AC-4: Targeted TensorizeIO test passed in review.
- AC-5: Round 1 now covers full/tail masked gather/scatter semantics and symbol mismatch messaging; accepted as re-verified.
- AC-6: Still incomplete because backend emission and dumps are not derived from actual lowered IR.

## Goal Tracker Update Decision

Claude's requested update is partially approved.

Approved:
- Marked task10 re-verified in round 1 with the new `UnitTestNTTAffineIOLowering` evidence.

Rejected:
- Did not mark task12 complete. The AST descriptor does not satisfy the required post-TTIR/native IR structural validation.
- Did not mark task13 complete. The final CUDA validation was not reproducible in this review environment, and dump artifacts are still descriptor summaries.

I updated `goal-tracker.md` mutable sections accordingly: task12 and task13 are active again, round 1 plan evolution was recorded, and open issues were added for the IR-recognition gap, fake dump artifacts, and CUDA validation blocker.

## Validation Run During Review

- Passed: `conda run -n flaglang python -m pytest python/test/unit/runtime/test_flaglang_cuda_backend.py python/test/unit/runtime/test_flaglang_native_ir.py -q` -> `6 passed, 2 skipped`
- Passed after serial rerun: `dotnet test src/Nncase.Tests/Nncase.Tests.csproj -s test.runsettings --filter "FullyQualifiedName~UnitTestNTTAffineIOLowering"` -> `5 passed`
- Passed: `dotnet test src/Nncase.Tests/Nncase.Tests.csproj -s test.runsettings --filter "FullyQualifiedName~UnitTestTensorizeIO"` -> `6 passed`
- Passed: import path and `ldd` checks point to this checkout and resolve `libnncaseruntime.so`
- Blocked: forced tutorial/unit benchmark validation because CUDA is unavailable in the current review environment
<!-- CODEX's REVIEW RESULT  END  -->
---

## Goal Tracker Reference (READ-ONLY after Round 0)

Before starting work, **read** @/mnt/home-nas/work/repo/FlagLang/.humanize/rlcr/2026-04-27_07-06-00/goal-tracker.md to understand:
- The Ultimate Goal and Acceptance Criteria you're working toward
- Which tasks are Active, Completed, or Deferred
- Any Plan Evolution that has occurred
- Open Issues that need attention

**IMPORTANT**: You CANNOT directly modify goal-tracker.md after Round 0.
If you need to update the Goal Tracker, include a "Goal Tracker Update Request" section in your summary (see below).

---

Note: You MUST NOT try to exit by lying, editing loop state files, or executing `cancel-rlcr-loop`.

After completing the work, please:
0. If the `code-simplifier` plugin is installed, use it to review and optimize your code. Invoke via: `/code-simplifier`, `@agent-code-simplifier`, or `@code-simplifier:code-simplifier (agent)`
1. Commit your changes with a descriptive commit message
2. Write your work summary into @/mnt/home-nas/work/repo/FlagLang/.humanize/rlcr/2026-04-27_07-06-00/round-2-summary.md

## Task Tag Routing Reminder

Follow the plan's per-task routing tags strictly:
- `coding` task -> Claude executes directly
- `analyze` task -> execute via `/humanize:ask-codex`, then integrate the result
- Keep Goal Tracker Active Tasks columns `Tag` and `Owner` aligned with execution

**If Goal Tracker needs updates**, include this section in your summary:
```markdown
## Goal Tracker Update Request

### Requested Changes:
- [E.g., "Mark Task X as completed with evidence: tests pass"]
- [E.g., "Add to Open Issues: discovered Y needs addressing"]
- [E.g., "Plan Evolution: changed approach from A to B because..."]
- [E.g., "Defer Task Z because... (impact on AC: none/minimal)"]

### Justification:
[Explain why these changes are needed and how they serve the Ultimate Goal]
```

Codex will review your request and update the Goal Tracker if justified.

#include <assert.h>

void sqrt(int x){
    assert(0 && "Unexpected execute sqrt function\n");
}
void floor(int x){
    assert(0 && "Unexpected execute floor function\n");
}
void fmin(int x){
    assert(0 && "Unexpected execute fmin function\n"); 
}
void fmax(int x){
    assert(0 && "Unexpected execute fmax function\n");     
}
void ceil(int x){
    assert(0 && "Unexpected execute fmax function\n");   
}
